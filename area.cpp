#include <bits/stdc++.h>
using namespace std;
float area(int &r){ 
    float calculate= 3.14*r*r;
    return calculate;
    }
    int main(){
        int b;
        cin >>b;
        cout<<"area is :"<< area(b);
        return 0;
        }
